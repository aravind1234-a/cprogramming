#include<stdio.h>
void main()
{
   int my=12;
   printf("this is an integer: %d \n",my);

   float a=5.77668;
   printf("this is a float val: %f \n",a);

   char b='g';
   printf("this is a character: %c \n",b);

   char t[10]="value";
   printf("this ia a string: %s",t);
}
// variable cannot start with a number
// variables cannot start with special char or special symbols but _ can be used
// variable - variable is a keyword whose value changes throughout the prog
// constnt - constant is a keyword whose value does not changes throughout the prog