#include<stdio.h>
#include<math.h>
void main()
{
    float n;
    float sq;
    printf("\n enter the number for which square root should be found: ");
    scanf("%f",&n);
    sq=sqrt(n);
    printf("the square root of %f= %f",n,sq);
}
// sqrt(number) -squrqe root 
// pow(number, power)  - power of a number
// sin(value) - sin of the value
// cos(value) - cos of the value